using UnityEngine;

namespace Gamer.Core
{
    public static partial class TextureUtils
    {
        public static void PlatformCompressTexture(Texture2D texture, TextureFormat format, TextureCompressionQuality quality = TextureCompressionQuality.Normal)
        {
            //CompressTexture(texture, TextureFormat.ETC2_RGB, quality);
        }
    }
}